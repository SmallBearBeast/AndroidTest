package com.example.administrator.androidtest.widget.stateful

interface IStateful2Text {
    fun setTextSpacing(spacing: Float)

    fun setPressedTextSpacing(spacing: Float)

    fun setSelectedTextSpacing(spacing: Float)
}